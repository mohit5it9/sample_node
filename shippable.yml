# Language setting
language: node_js


# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU

  matrix:
    - M1=ONE
    - M2=TWO

build:
  pre_ci:
    - echo "Good morning 2"
    - export IMAGE_TAG="driftking"
    #- if [ "$BRANCH" == "master" ] || [ "$BRANCH" == "staging" ] || [ "$BRANCH" == "develop" ] ; then export BUILD_TASK="build" ; else export BUILD_TASK="test" ; fi
    - echo $IMAGE_TAG
    #- echo $BUILD_TASK

  pre_ci_boot:
    image_name: scriptnull/debian
    image_tag: $IMAGE_TAG
    pull: true
    options: "-e HOME=/root"
    #- echo $IMAGE_TAG

  ci:
    - npm install
